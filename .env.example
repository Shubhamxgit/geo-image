# Optional: Google API Key that has Geocoding and Static Maps enabled.
# If you provide this key in a local .env (VITE_GOOGLE_MAPS_KEY), the app will use Google Maps
# for reverse geocoding and static thumbnails (better POI/landmark results).
# If you don't set this, the app will fall back to OpenStreetMap (free).
VITE_GOOGLE_MAPS_KEY=